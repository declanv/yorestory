<%= form_for milestone_local do |f| %>
  <%= f.text_field :title, { placeholder: "title" } %>
  <%= f.text_field :content, { placeholder: "content" } %>
  <%= f.hidden_field :timeline_id, value: timeline_id %>
  <%= f.submit %>
<% end %>
